import { Text, View } from "react-native";

interface SelectedLocationPlacesProps {
    id: string
}

export default function SelectedLocationPlaces({ id }: SelectedLocationPlacesProps) {
    return (
        <View>
            <Text>SelectedLocationPlaces</Text>
        </View>
    )
}
